#include "order_test.lp".

% move(TRAIN_ID, ((X,Y), DIR), ((NEXT_X,NEXT_Y), NEXT_DIR))         TRAIN_ID moves between
% resolve(ID1, ID2, U)

% --- [ START ] --------------------------------------------------------------------------------------------------------

% Trains start at earliest departure in their spawning locations off the map

% Go forward if next cell is not a juncture

% If next cell is a juncture, check resul


